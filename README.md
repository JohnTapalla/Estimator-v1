# Estimator App (Vite + React + Tailwind)

## Run locally

```bash

npm install

npm run dev

```

## Build

```bash

npm run build

npm run preview

```

## Configure without code

Edit `public/app-config.json` and refresh.